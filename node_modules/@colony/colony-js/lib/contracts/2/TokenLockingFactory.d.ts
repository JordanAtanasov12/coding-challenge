import { ContractFactory, Signer } from "ethers";
import { Provider } from "ethers/providers";
import { UnsignedTransaction } from "ethers/utils/transaction";
import { TransactionOverrides } from ".";
import { TokenLocking } from "./TokenLocking";
export declare class TokenLockingFactory extends ContractFactory {
    constructor(signer?: Signer);
    deploy(overrides?: TransactionOverrides): Promise<TokenLocking>;
    getDeployTransaction(overrides?: TransactionOverrides): UnsignedTransaction;
    attach(address: string): TokenLocking;
    connect(signer: Signer): TokenLockingFactory;
    static connect(address: string, signerOrProvider: Signer | Provider): TokenLocking;
}
